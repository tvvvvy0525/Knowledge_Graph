### 1. 后端配合需求 (Backend Requirements)

#### 1.1 节点与关系的数据结构约定
前端 ECharts 需要的字段：
*   **节点 (Node)**: `{ "id": "uuid", "name": "显示名称", "category": "类型", "symbolSize": 30 }`
*   **关系 (Link)**: `{ "source": "id1", "target": "id2", "name": "关系名" }`

#### 1.2 必需接口
**接口 1: 获取初始图谱**
*   **URL**: `GET /api/graph/init`
*   **响应**:
    ```json
    {
      "code": 200,
      "data": {
        "nodes": [
          { "id": "1", "name": "松材线虫病", "category": "病害", "symbolSize": 50 },
          { "id": "2", "name": "松墨天牛", "category": "昆虫", "symbolSize": 30 }
        ],
        "links": [
          { "source": "2", "target": "1", "name": "传播媒介" }
        ]
      }
    }
    ```

**接口 2: 获取邻居节点 (用于展开)**
*   **URL**: `GET /api/graph/neighbors/{id}`
*   **响应**: 同上，返回该节点及其直接连接的邻居和连线。
