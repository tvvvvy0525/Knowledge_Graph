# 松材线虫病知识系统技术规格说明书
**Technical Specification for Pine Wilt Disease Knowledge System**

**日期**: 2025-11-26  
**项目目标**: 构建一个集知识图谱可视化、管理、搜索、诊断与问答于一体的综合系统。

---

## 1. 必做功能模块 (Core Modules)

### 1.1 知识图谱可视化 (Visualization)
*   **功能描述**:
    *   **力导向图**: 使用 ECharts 展示实体（节点）与关联（边），支持拖拽布局。
    *   **节点分类配色**: 根据实体类型（松材线虫、媒介昆虫、寄主植物、环境因素）自动渲染不同颜色。
    *   **动态交互**:
        *   **点击展开**: 点击节点，动态加载并显示其直接关联的邻居节点。
        *   **点击收起**: 再次点击，隐藏仅与该节点相连的叶子节点。
        *   **悬停提示 (Tooltip)**: 鼠标悬停显示节点名称及分类。
    *   **侧边详情面板**: 点击节点/关系，右侧滑出面板，展示详细属性（描述、图片、权重等）。

### 1.2 知识图谱管理 (CRUD)
*   **功能描述**: 实现了对知识库的增删改查，不仅仅是只读展示。
    *   **新建实体**: 全局入口，填写名称、分类、描述后创建新节点。
    *   **编辑实体**: 在详情面板中修改属性并保存。
    *   **新建关系**: 选中节点 -> 搜索目标节点 -> 建立特定类型的关联。
    *   **删除实体**: 级联删除节点及其所有关联关系。

### 1.3 智能语义搜索 (Search)
*   **功能描述**:
    *   **模糊检索**: 输入关键词（如“松墨”），返回所有匹配节点。
    *   **图谱联动**: 搜索选中后，清空当前画布，聚焦展示目标节点及其 1 度关联子图。

---

## 2. 选做功能模块 (Optional Modules)

### 2.1 智能诊断专家系统 (Expert Diagnosis)
*   **功能描述**: 基于《松材线虫病入侵危害度定量评估表》(表 2-1) 实现。
    *   **多因子计算**: 系统后台内置复杂的几何平均与算术平均公式。
    *   **向导式问卷**: 前端根据后台配置的因子树，分步骤引导用户回答问题。
    *   **风险评估**: 提交后系统计算 $D_0$ 值，给出风险等级及扣分依据。

### 2.2 领域知识问答 (AI QA)
*   **功能描述**:
    *   **RAG (检索增强生成)**: 系统拦截用户提问，先在 Neo4j 中检索相关三元组，将其作为上下文喂给大语言模型 (LLM)。
    *   **对话界面**: 类似 ChatGPT 的对话气泡窗口。

---

## 3. 技术架构规范

### 3.1 总体架构
*   **前端**: Vue 3 + Vite + Pinia + Element Plus + ECharts 5
*   **后端**: Spring Boot 3.x + Spring Data Neo4j + LangChain4j
*   **数据库**: Neo4j Community Server 5.x

### 3.2 数据模型 (Neo4j Schema)

| 类型 | 标签/类型 | 属性 (Properties) | 说明 | 归属模块 |
| :--- | :--- | :--- | :--- | :--- |
| **Node** | `Entity` | `id`, `name`, `category`, `description`, `image` | 通用知识实体 | 必做 |
| **Node** | `DiagnosisFactor` | `code` (I1), `weight`, `formula` | 诊断因子定义 | 选做 |
| **Node** | `DiagnosisOption` | `text`, `score` | 评分选项 | 选做 |
| **Edge** | `RELATED_TO` | `type` (传播/感染) | 通用关系 | 必做 |
| **Edge** | `HAS_FACTOR` | - | 因子层级结构 | 选做 |

---

## 4. API 接口规范 (RESTful)

### 必做接口
| 方法 | URL | 描述 |
| :--- | :--- | :--- |
| `GET` | `/api/graph/init` | 获取初始图谱数据 |
| `GET` | `/api/graph/neighbors/{id}` | 获取节点邻居（用于展开） |
| `GET` | `/api/graph/search?q={kw}` | 模糊搜索 |
| `POST` | `/api/manage/node` | 创建新节点 |
| `PUT` | `/api/manage/node` | 更新节点属性 |
| `DELETE` | `/api/manage/node/{id}` | 删除节点 (级联) |
| `POST` | `/api/manage/relation` | 创建关系 |

### 选做接口
| 方法 | URL | 描述 |
| :--- | :--- | :--- |
| `GET` | `/api/diagnosis/questions` | 获取诊断问卷结构 |
| `POST` | `/api/diagnosis/submit` | 提交评分并获取结果 |
| `POST` | `/api/chat/ask` | AI 问答交互 |

---

## 5. 实施检查清单 (Checklist)

**Phase 1: 基础搭建 (必做)**
1.  [ ] Spring Boot 项目初始化 (Web, Neo4j, Lombok)。
2.  [x] Vue 3 项目初始化 (Router, Pinia, Axios, ElementPlus, ECharts)。
3.  [ ] Neo4j 数据库连接配置。
4.  [ ] 编写 `OwlDataSeeder`，实现基础数据的模拟入库。

**Phase 2: 图谱可视化与交互 (必做)**
5.  [ ] 后端: 实现 `/init` 和 `/neighbors` 接口。
6.  [x] 前端: 封装 `GraphChart.vue` 组件。
7.  [x] 前端: 实现 Pinia Store (`useGraphStore`) 管理数据。
8.  [x] 前端交互: 实现点击展开/收起逻辑。
9.  [x] 前端交互: 实现 Tooltip 悬停显示。
10. [ ] 前端交互: 开发 `InfoPanel.vue` (侧边栏) 显示详情。

**Phase 3: 核心 CRUD (必做)**
11. [ ] 后端: 实现增删改接口 (`/manage/**`)。
12. [ ] 前端: 开发“新建实体”弹窗。
13. [ ] 前端: `InfoPanel` 增加“编辑模式”切换。
14. [ ] 前端: `InfoPanel` 增加“添加关联”功能。
15. [ ] 前端: `InfoPanel` 增加“删除”按钮。

**Phase 4: 搜索 (必做)**
16. [ ] 后端: 实现 `/search` 接口。
17. [ ] 前端: 顶部导航栏实现搜索框联动。

**Phase 5: 智能诊断 (选做)**
18. [ ] 后端: 初始化表 2-1 数据结构。
19. [ ] 后端: 编写评分计算算法。
20. [ ] 前端: 开发 `DiagnosisView` 向导页面。

**Phase 6: 智能问答 (选做)**
21. [ ] 后端: 集成 LangChain4j。
22. [ ] 前端: 开发 `ChatView` 聊天页面。
