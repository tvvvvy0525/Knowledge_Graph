## 项目开发进度文档

### 一、 项目概述

本项目是一个基于知识图谱的松材线虫病知识系统。系统采用前后端分离的架构，后端负责图谱数据的管理和API服务，前端负责图谱的可视化展示和交互操作。

**技术栈:**

-   **后端:** Java 21, Spring Boot 3.2.4, Neo4j (图数据库)
-   **前端:** Vue 3, Vite, Pinia (状态管理), ECharts (图表库), Element Plus (UI组件库)

### 二、 后端开发进度

后端服务已经基本完成，提供了稳定、完整的RESTful API接口，用于支撑前端的各项功能。

**1. 核心功能:**

-   **图数据模型:** 定义了知识图谱中的核心实体 (`EntityNode`)，包含ID、中英文名称、类别、描述等属性。
-   **数据库集成:** 成功集成Neo4j图数据库，通过Spring Data Neo4j进行数据操作，配置文件位于 `application.yml`。
-   **API接口:**
    -   **图谱查询接口 (`GraphController`):**
        -   `GET /api/graph/neighbors/{id}`: 获取指定节点的邻居节点和关系，是前端展开/收起功能的核心。
        -   `GET /api/graph/search`: 根据关键词模糊搜索节点，返回匹配的子图。
        -   `GET /api/graph/relation/types`: 根据关键词搜索关系类型。
    -   **图谱管理接口 (`ManageController`):**
        -   `POST /api/manage/node`: 创建新节点。
        -   `PUT /api/manage/node`: 更新已有节点信息。
        -   `DELETE /api/manage/node/{id}`: 删除节点及其关联关系。
        -   `POST /api/manage/relation`: 创建节点之间的关系。

**2. 完成状态:**

-   所有核心API均已开发完成并通过测试。
-   数据模型稳定。
-   项目依赖清晰，使用Maven管理。

### 三、 前端开发进度

前端应用负责数据的可视化和用户交互，目前核心功能也已完成，能够与后端API良好配合，实现一个功能完善的知识图谱系统。

**1. 核心功能:**

-   **图谱可视化:**
    -   使用 **ECharts** 作为核心渲染引擎，在 `GraphChart.vue` 组件中实现了力引导布局的知识图谱。
    -   能够动态渲染节点和关系，并根据节点类别自动分配不同颜色。
    -   支持图谱的缩放、平移等基本交互。
-   **核心交互逻辑:**
    -   **初始化加载:** 页面加载时，调用后端接口加载以根节点(ID=1)为中心的初始图谱。
    -   **节点展开/收起:** 单击节点可异步加载其邻居节点，实现图谱的探索式浏览。再次单击已展开的节点可收起其叶子邻居，简化视图。
    -   **节点搜索:** `SearchBar.vue` 组件提供了搜索功能，用户输入关键词后，调用搜索接口，并将结果在图谱中高亮展示。
    -   **信息展示:** `InfoPanel.vue` 组件用于展示当前选中节点的详细信息。
-   **图谱编辑:**
    -   **创建实体/关系:** 通过 `NodeCreateDialog.vue` 和 `LinkCreateDialog.vue` 弹窗组件，用户可以方便地向图谱中添加新的知识。
    -   **编辑/删除:** 在信息展示面板 (`InfoPanel.vue`) 中，提供了对选中节点的编辑和删除功能。
-   **状态管理:**
    -   使用 **Pinia** (`graphStore.js`) 集中管理图谱的节点、关系、加载状态等数据，使得组件间数据流清晰、可控。

**2. 完成状态:**

-   主视图 (`HomeView.vue`) 布局合理，整合了搜索、图谱展示和信息面板三大功能区。
-   组件化程度高，逻辑清晰。
-   与后端API的对接已全部完成。

### 四、 总体进度评估

项目整体已进入功能完善阶段。前后端核心功能均已实现，并且可以联调通过，形成了一个完整的业务闭环。

**后续工作建议:**

1.  **功能优化:**
    -   对图谱大数据量下的渲染性能进行测试和优化。
    -   优化搜索结果的展示方式，例如高亮中心节点。
2.  **用户体验提升:**
    -   细化错误提示和加载状态的反馈。
    -   对UI样式进行微调，提升美观度。
3.  **部署:**
    -   准备前后端的生产环境打包和部署工作。
