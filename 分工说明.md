# 前端开发分工协作指南 (完全解耦版)

## 1. 👥 角色职责与文件领地

| 成员 | 职责方向 | 独占的文件/组件 | 功能入口位置 |
| :--- | :--- | :--- | :--- |
| **Member A** | **增 + 查** | `SearchBar.vue`,`NodeCreateDialog.vue`,`LinkCreateDialog.vue`  | **顶部导航栏 (Header)** |
| **Member B** | **改 + 删** | `InfoPanel.vue` | **右侧详情面板 (Sidebar)** |

---

## 2. 📂 详细任务清单

### 🧑‍💻 Member A：构建者 (Builder)
**任务目标**：负责所有“新数据产生”和“数据检索”的入口。

#### 任务 A1: 顶部搜索 (`SearchBar.vue`)
*   **位置**: Header 中间。
*   **逻辑**: 输入关键词 -> 调用 Store `searchNode` -> 更新视图。

#### 任务 A2: 新建节点 (`NodeCreateDialog.vue`)
*   **位置**: Header 右侧按钮 “新建实体”。
*   **UI**: 弹窗，表单包含 Name, Category, Desc。
*   **逻辑**: 提交 -> 调用 Store `addNode`。

#### 任务 A3: 新建关系 (`LinkCreateDialog.vue`) —— *关键变动*
*   **位置**: Header 右侧按钮 “新建关联” (在新建实体旁边)。
*   **UI**: 一个独立的弹窗。
    *   **源节点 (Source)**: 下拉选择框 (支持搜索)。
    *   **目标节点 (Target)**: 下拉选择框 (支持搜索)。
    *   **关系名称**: 输入框。
*   **逻辑**: 提交 -> 调用 Store `addLink`。
*   *优势*: 这样 A 不需要去改 B 的侧边栏，也不依赖当前选中的节点，逻辑更独立。

---

### 🧑‍💻 Member B：管理者 (Manager)
**任务目标**：负责对“已有数据”的维护。

#### 任务 B1: 属性编辑 (`InfoPanel.vue`)
*   **位置**: 右侧面板顶部。
*   **逻辑**:
    *   增加“编辑”图标按钮。
    *   点击切换为表单模式 (Input框)。
    *   保存 -> 调用 Store `updateNode`。

#### 任务 B2: 删除节点 (`InfoPanel.vue`)
*   **位置**: 右侧面板底部。
*   **逻辑**:
    *   增加“删除”红色按钮。
    *   确认后 -> 调用 Store `deleteNode` -> 关闭面板。

---

## 3. 🚀 协作实施步骤 (无冲突版)

由于文件完全隔离，你们现在可以**同时开工**，不需要等待对方。

### Step 1: 约定 Store (共用部分)
请先将以下代码复制到 `src/stores/graphStore.js` 的 `actions` 部分（两人都加上，避免报错）：

```javascript
// graphStore.js 预留接口
actions: {
    // ...原有的 fetchInitGraph 等
    
    // Member A 用
    async searchNode(keyword) { console.log('Search:', keyword) },
    async addNode(node) { this.nodes.push(node) }, // 简易版 Mock
    async addLink(link) { this.links.push(link) },
    
    // Member B 用
    async updateNode(updatedNode) { 
        const index = this.nodes.findIndex(n => n.id === updatedNode.id)
        if (index !== -1) this.nodes[index] = { ...updatedNode }
    },
    async deleteNode(nodeId) {
        this.nodes = this.nodes.filter(n => n.id !== nodeId)
        this.links = this.links.filter(l => l.source !== nodeId && l.target !== nodeId)
    }
}
```

### Step 2: Member A 开发清单
1.  在 `src/components/` 下新建 `SearchBar.vue`, `NodeCreateDialog.vue`, `LinkCreateDialog.vue`。
2.  修改 `HomeView.vue` 的 `<div class="header">` 部分，把这三个组件放进去。

### Step 3: Member B 开发清单
1.  直接修改 `src/components/InfoPanel.vue`。
2.  在 `<template>` 里增加编辑态的 `v-if/else` 和底部的删除按钮。
