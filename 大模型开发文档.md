# 松材线虫病知识系统 - 智能问答模块开发技术文档

**版本**：v1.0  
**状态**：开发中  
**目标**：在现有图谱系统的基础上，引入**本地大模型 (Ollama)** 和 **向量检索 (Vector Search)**，实现专业的自然语言问答功能。

---

## 1. 项目现状与架构回顾

### 1.1 现有功能
目前项目已完成 **“知识图谱可视化与管理系统”** 的核心功能：
1.  **后端**：基于 Spring Boot + Neo4j，实现了实体的 CRUD 和图谱查询接口。
2.  **前端**：基于 Vue3 + ECharts，实现了力导向图展示、侧边栏详情编辑。
3.  **规则推理**：完成了“智能诊断向导（Wizard）”，基于定量评估模型计算风险等级。

### 1.2 现有代码结构
*   **后端 (`pinewilt-backend`)**：
    *   `model/EntityNode`: 图节点实体。
    *   `repository/EntityNodeRepository`: Neo4j 数据访问。
    *   `service/DiagnosisService`: 诊断算法逻辑。
*   **前端 (`pine-wilt-sys`)**：
    *   `components/GraphChart.vue`: 图谱渲染。
    *   `components/DiagnosisWizard.vue`: 诊断弹窗。
    *   `views/HomeView.vue`: 主页面布局。

### 1.3 存在的问题
目前的系统擅长处理**结构化数据**（如：A 是 B 的父类），但缺乏处理**非结构化文本**的能力。
*   *痛点*：用户如果问“《松材线虫病普查技术规程》里规定的取样方法是什么？”，图谱很难回答，因为这些是大段的文本。

---

## 2. 新功能：基于 RAG 的智能问答系统

### 2.1 技术方案 (Hybrid RAG)
我们将采用 **“双路召回 + 大模型润色”** 的架构：

1.  **知识库扩充**：构建一个 `knowledge_data.json` 文件，存储法规、标准、长文本描述。
2.  **向量存储**：利用 Neo4j 自带的 Vector Index 功能，将上述 JSON 数据转化为向量存储。
3.  **大模型 (LLM)**：使用 **Ollama** 运行本地模型（推荐 `qwen2.5:7b`），通过 **Spring AI** 进行调用。

### 2.2 数据流向图
```mermaid
用户提问 ("怎么取样？") 
   │
   ▼
[后端 Spring Boot]
   │
   ├──> 1. 向量检索 (Vector Search) ──> [Neo4j 向量索引] ──> 返回相似文档片段
   │
   ├──> 2. 提示词工程 (Prompt Engineering)
   │    "你是专家。参考资料：[文档片段]。回答用户：[问题]"
   │
   ▼
[Ollama 本地大模型]
   │
   ▼
生成回答 ("根据GB/T标准，应在树干上中下部...") ──> 返回前端
```

---

## 3. 详细实施步骤 (后端开发)

**负责人**：后端开发成员 A & B

### 3.1 环境准备 (Ollama)
1.  **下载安装**：[Ollama 官网](https://ollama.com)。
2.  **拉取模型**：在终端运行 `ollama pull qwen2.5:7b`。
3.  **创建定制模型**（可选，用于固定人设）：
    *   新建文件 `Modelfile`，内容：
        ```dockerfile
        FROM qwen2.5:7b
        SYSTEM "你是一个松材线虫病防治专家，回答请依据提供的上下文，保持学术严谨。"
        ```
    *   运行：`ollama create pwn-expert -f Modelfile`

### 3.2 引入依赖 (`pom.xml`)
在现有项目中加入 Spring AI 和 Neo4j Vector 支持。

```xml
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-bom</artifactId>
            <version>1.0.0-SNAPSHOT</version> <!-- 请检查最新版本 -->
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>

<dependencies>
    <!-- Spring AI Ollama -->
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-ollama-spring-boot-starter</artifactId>
    </dependency>
    <!-- Spring AI Neo4j Vector Store -->
    <dependency>
        <groupId>org.springframework.ai</groupId>
        <artifactId>spring-ai-neo4j-store-spring-boot-starter</artifactId>
    </dependency>
</dependencies>

<!-- 必须添加 Milestone 仓库 -->
<repositories>
    <repository>
        <id>spring-milestones</id>
        <url>https://repo.spring.io/milestone</url>
    </repository>
</repositories>
```

### 3.3 配置文件 (`application.yml`)
```yaml
spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: pwn-expert # 或 qwen2.5:7b
    vectorstore:
      neo4j:
        uri: bolt://localhost:7687
        username: neo4j
        password: your_password
        embedding-dimension: 1024 # 对应 qwen2.5 的维度，需查阅模型说明
        index-name: custom-vector-index
```

### 3.4 核心代码实现

#### A. 数据加载器 (`VectorStoreLoader.java`)
用于在启动时将 JSON 数据导入 Neo4j。

```java
@Component
public class VectorStoreLoader implements CommandLineRunner {
    @Autowired
    private VectorStore vectorStore;
    @Value("classpath:knowledge_data.json")
    private Resource jsonFile;

    @Override
    public void run(String... args) throws Exception {
        // 读取 JSON -> 转为 List<Document> -> vectorStore.add(docs);
        // 具体代码参考聊天记录中的实现
    }
}
```

#### B. 聊天服务 (`PwdChatService.java`)
```java
@Service
public class PwdChatService {
    private final ChatClient chatClient;
    private final VectorStore vectorStore;

    // 构造注入...

    public String chat(String query) {
        // 1. 检索相似文档 (Top 2)
        List<Document> docs = vectorStore.similaritySearch(
            SearchRequest.query(query).withTopK(2)
        );
        String context = docs.stream().map(Document::getContent).collect(Collectors.joining("\n"));

        // 2. 构造 Prompt
        String prompt = "参考资料：\n" + context + "\n问题：" + query;

        // 3. 调用 AI
        return chatClient.prompt().user(prompt).call().content();
    }
}
```

---

## 4. 详细实施步骤 (数据准备)

**负责人**：全员参与（或文档整理员）

在 `src/main/resources/` 下创建 `knowledge_data.json`。格式如下：

```json
[
  {
    "instruction": "松材线虫病的取样部位在哪里？",
    "output": "根据GB/T 23476标准，应在树干的下部、中部和上部变色部位分别取样。取样深度需达到木质部。"
  },
  {
    "instruction": "如何辨别松树是否感病？",
    "output": "主要特征包括：1. 针叶失水萎蔫，呈红褐色，且不脱落；2. 树脂分泌减少或停止（蓝变现象）；3. 树干有天牛羽化孔。"
  },
  {
    "instruction": "疫木如何处理？",
    "output": "疫木必须进行除害处理，常用的方法包括：粉碎（切片厚度小于1cm）、烧毁、削片或钢丝网罩处理。"
  }
]
```
*建议扩充至 30-50 条，覆盖：生物学特性、检疫标准、防治药剂、法律责任。*

---

## 5. 详细实施步骤 (前端开发)

**负责人**：前端开发成员

### 5.1 新增聊天组件
新建 `src/components/AIChatBox.vue`。
*   **UI设计**：页面右下角的悬浮按钮（Floating Action Button）。
*   **交互**：点击展开聊天窗口，类似微信对话界面。
*   **逻辑**：调用后端 `/api/chat/ask` 接口，展示 Loading 状态。

### 5.2 集成到主页
修改 `src/views/HomeView.vue`：
```html
<template>
  <div class="home-container">
    <!-- 原有内容 -->
    ...
    <!-- 新增组件 -->
    <AIChatBox />
  </div>
</template>
```

---

## 6. 任务分工与时间表 (Timeline)

| 任务 | 负责人 | 预计工时 | 交付物 |
| :--- | :--- | :--- | :--- |
| **环境搭建** | 后端 A | 2小时 | Ollama 运行成功，Spring AI 依赖配置完成 |
| **数据整理** | 后端 B / 全员 | 4小时 | `knowledge_data.json` (含50条高质量问答) |
| **后端接口** | 后端 A | 4小时 | `/api/chat/ask` 接口跑通 (能返回 AI 回答) |
| **前端组件** | 前端 A | 4小时 | `AIChatBox.vue` 界面美观，交互流畅 |
| **联调测试** | 全员 | 2小时 | 提问测试，确保 AI 不胡说八道 |

---

## 7. 常见问题 (FAQ)

1.  **Q: Neo4j 需要装插件吗？**
    *   A: Neo4j 5.x 版本原生支持 Vector Index，一般不需要额外插件。如果报错，请检查 Neo4j 版本。
2.  **Q: Ollama 跑起来电脑很卡怎么办？**
    *   A: `qwen2.5:7b` 需要约 8GB 内存。如果卡顿，可以尝试更小的模型如 `qwen2.5:1.5b` 或 `llama3.2:3b`，虽然变笨一点，但速度快。
3.  **Q: 为什么 AI 回答不知道图谱里的节点？**
    *   A: 目前这个方案是基于 `json` 文本检索的。如果想让 AI 知道图谱结构，需要在 Prompt 里拼接一些图谱查询结果（Phase 2 优化方向）。

---